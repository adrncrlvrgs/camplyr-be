import app from "./app";

const PORT = process.env.PORT;

app.listen(PORT, () => {
  console.log(`Server is now running at ${PORT}`);
});


// to do
// connect be and fe
// create initaial working login
// landing page
